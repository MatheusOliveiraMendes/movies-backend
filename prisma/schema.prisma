

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Movie {
  id          Int      @id
  key         String
  name        String
  description String
  genres      String[] 
  rate        Float
  length      String
  img         String
}
